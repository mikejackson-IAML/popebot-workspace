{
  "title": "Rewrite state.ts with typed maps, cascade deletes, and new methods",
  "job": "Rewrite plugins/dev-department/src/worker/state.ts to fix critical issues found in code review.\n\nRead the existing file first, then replace it entirely with these fixes:\n\n1. Replace Map<string, any> with separate typed maps:\n   private projects: Map<string, DevProject> = new Map()\n   private phases: Map<string, Phase> = new Map()\n   private specs: Map<string, Spec> = new Map()\n   private prds: Map<string, PRD> = new Map()\n   private conversationRefs: Map<string, ConversationReference> = new Map()\n   private buildDispatches: Map<string, BuildDispatch> = new Map()\n   private buildOutputs: Map<string, BuildOutput> = new Map()\n   private reviews: Map<string, Review> = new Map()\n   private revisionEvents: Map<string, RevisionEvent> = new Map()\n\n2. Use crypto.randomUUID() for ALL ID generation consistently\n\n3. Add cascade delete:\n   deleteProject(id) should also delete all phases for that project, and each phase deletion should cascade to its specs, prds, and conversation refs\n   deletePhase(id) should delete associated specs, prds, and conversation refs\n\n4. Add deleteSpec(id) and deletePRD(id) methods\n\n5. Add stub read methods for Phase 2 entities:\n   getBuildDispatchByPhase(phaseId): returns BuildDispatch or null\n   getBuildOutputByPhase(phaseId): returns BuildOutput or null\n   getReviewByPhase(phaseId): returns Review or null\n   getRevisionEventsByPhase(phaseId): returns RevisionEvent[]\n\n6. Import ALL types from ./types including BuildDispatch, BuildOutput, Review, RevisionEvent\n\n7. Export singleton as store\n\nCommit the rewritten file."
}