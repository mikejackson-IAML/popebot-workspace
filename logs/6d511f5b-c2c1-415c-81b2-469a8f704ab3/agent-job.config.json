{
  "title": "Add state machine enforcement with valid transitions to worker state file",
  "job": "Edit plugins/dev-department/src/worker/state.ts to add state machine enforcement.\n\nRead the existing file first. Add a VALID_TRANSITIONS map at the top of the file after imports:\n\nconst VALID_TRANSITIONS: Record<PhaseStatus, PhaseStatus[]> = {\n  DraftSpec: ['SpecApproved'],\n  SpecApproved: ['PRDAttached', 'DraftSpec'],\n  PRDAttached: ['ReadyForBuild', 'SpecApproved'],\n  ReadyForBuild: ['SentToBuilder', 'PRDAttached'],\n  SentToBuilder: ['BuildInProgress'],\n  BuildInProgress: ['BuildReturned'],\n  BuildReturned: ['UnderVerification'],\n  UnderVerification: ['UnderReview', 'ReworkRequired'],\n  UnderReview: ['Accepted', 'AcceptedWithDownstreamRevisions', 'ReworkRequired'],\n  Accepted: ['Closed'],\n  AcceptedWithDownstreamRevisions: ['Closed'],\n  ReworkRequired: ['DraftSpec', 'PRDAttached'],\n  Closed: [],\n};\n\nWait — the PhaseStatus type only has: DraftSpec, SpecApproved, PRDAttached, ReadyForBuild, Accepted, ReworkRequired, Closed. So use those values only:\n\nconst VALID_TRANSITIONS: Record<PhaseStatus, PhaseStatus[]> = {\n  DraftSpec: ['SpecApproved'],\n  SpecApproved: ['PRDAttached', 'DraftSpec'],\n  PRDAttached: ['ReadyForBuild', 'SpecApproved'],\n  ReadyForBuild: ['Accepted', 'ReworkRequired'],\n  Accepted: ['Closed'],\n  ReworkRequired: ['DraftSpec', 'PRDAttached'],\n  Closed: [],\n};\n\nIn the updatePhase method, add validation BEFORE applying the update:\nif (data.status && data.status !== existing.status) {\n  const allowed = VALID_TRANSITIONS[existing.status];\n  if (!allowed.includes(data.status)) {\n    throw new Error('Invalid status transition from ' + existing.status + ' to ' + data.status);\n  }\n}\n\nAlso import PhaseStatus from ./types if not already imported.\n\nExport VALID_TRANSITIONS so the UI can read it.\n\nCommit."
}