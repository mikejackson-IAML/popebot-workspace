{
  "title": "Add persistence methods and dirty tracking to StateStore class",
  "job": "Edit plugins/dev-department/src/worker/state.ts to add persistence support.\n\nRead the existing file first. Add these methods to the StateStore class:\n\n1. Add a private serialization method:\n  private serialize(): string {\n    return JSON.stringify({\n      projects: Object.fromEntries(this.projects),\n      phases: Object.fromEntries(this.phases),\n      specs: Object.fromEntries(this.specs),\n      prds: Object.fromEntries(this.prds),\n      conversationRefs: Object.fromEntries(this.conversationRefs),\n      buildDispatches: Object.fromEntries(this.buildDispatches),\n      buildOutputs: Object.fromEntries(this.buildOutputs),\n      reviews: Object.fromEntries(this.reviews),\n      revisionEvents: Object.fromEntries(this.revisionEvents),\n    });\n  }\n\n2. Add hydrate method that loads from a JSON string:\n  hydrate(json: string): void {\n    const data = JSON.parse(json);\n    // Rebuild each map from the parsed data\n  }\n\n3. Add flush method that returns the serialized state:\n  flush(): string {\n    return this.serialize();\n  }\n\n4. Add a dirty flag that tracks whether state has changed since last flush:\n  private dirty = false;\n  Mark dirty=true in every create/update/delete method.\n  Add isDirty() getter.\n  Reset dirty=false in flush().\n\nKeep ALL existing methods. Only add new ones. Commit."
}