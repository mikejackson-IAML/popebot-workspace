{
  "title": "Read messages and state files to rewrite worker index with message handler",
  "job": "Rewrite plugins/dev-department/src/worker/index.ts to add a message handler.\n\nRead messages.ts and state.ts first. The new index.ts should:\n\n1. Import store from ./state\n2. Import WorkerRequest, WorkerResponse from ./messages\n3. Re-export all types from ./types and ./messages\n4. Export a handleMessage function:\n  export function handleMessage(request: WorkerRequest): WorkerResponse {\n    switch (request.type) {\n      case 'init': return { type: 'projects', data: store.listProjects() };\n      case 'createProject': return { type: 'project', data: store.createProject(request.data) };\n      case 'updateProject': return { type: 'project', data: store.updateProject(request.id, request.data) };\n      case 'deleteProject': store.deleteProject(request.id); return { type: 'ok' };\n      case 'listProjects': return { type: 'projects', data: store.listProjects() };\n      // ... handle all other message types\n      case 'dispatchBuild': // Create BuildDispatch record, return it\n      default: return { type: 'error', message: 'Unknown request type' };\n    }\n  }\n5. Export store for direct access if needed\n\nCommit."
}